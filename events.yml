cassandra:
  contactpoints: 127.0.0.1
  port: 9042
  keyspace: myKeyspace
  basepackages: com.eventstore
  startup:
    script: CREATE KEYSPACE IF NOT EXISTS ${cassandra.keyspace} WITH replication = {‘class’:’SimpleStrategy’, ‘replication_factor’ : 1};  
